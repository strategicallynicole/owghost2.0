<!DOCTYPE html>
<html class="wide wow-animation" lang="{{@site.lang}}">
    <head>    <script src="{{asset "js/particles.js"}}"></script>

        <!-- Site Title-->
        <title>Classic Blog</title>
        {{> "tech-meta"}}
        {{> "stylesheets"}}
        {{ghost_head}}
        {{> "iecss"}}
    </head>
    <body class="{{body_class}}">
        {{> "pre-loader"}}
        {{> "iewarning"}}
        {{{body}}}       
    </body>
    
</html>
